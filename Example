ArsenalAPI = "https://raw.githubusercontent.com/MAJESTY5164/ArsenalGunModding/refs/heads/main/Source.lua"
loadstring(game:HttpGet(ArsenalAPI))()

Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
Window = Rayfield:CreateWindow({
    Name = "Arsenal API Gui",
    Icon = 0, -- Icon in Topbar. Can use Lucide Icons (string) or Roblox Image (number). 0 to use no icon (default).
    LoadingTitle = "Arsenal API",
    LoadingSubtitle = "made by MAJESTY",
    Theme = "Default", -- Check https://docs.sirius.menu/rayfield/configuration/themes
    
    DisableRayfieldPrompts = true,
    DisableBuildWarnings = true, -- Prevents Rayfield from warning when the script has a version mismatch with the interface
    
    ConfigurationSaving = {
    Enabled = false,
    FolderName = nil, -- Create a custom folder for your hub/game
    FileName = "Big Hub"
    },
    
    Discord = {
    Enabled = false, -- Prompt the user to join your Discord server if their executor supports it
    Invite = "noinvitelink", -- The Discord invite code, do not include discord.gg/. E.g. discord.gg/ABCD would be ABCD
    RememberJoins = true -- Set this to false to make them join the discord every time they load it up
    },
    
    })

Main = Window:CreateTab("Main", "info")
Appearance = Window:CreateTab("Appearance", "info")
Misc = Window:CreateTab("Misc", "info")

Appearance:CreateButton({
    Name = ("List Melees"),
    Callback = function()
        getgenv().ListMelee()
        game:GetService("StarterGui"):SetCore("DevConsoleVisible", true)
    end,
    })

SetKnife = game:GetService("Players").LocalPlayer.Data.Melee.Value

Appearance:CreateInput({
   Name = "Set Melee",
   CurrentValue = "",
   PlaceholderText = "Enter a Melee",
   RemoveTextAfterFocusLost = false,
   Flag = "Input1",
   Callback = function(Text)
    SetKnife = Text
   end,
})

Appearance:CreateButton({
    Name = ("Apply Melee"),
    Callback = function()
        getgenv().SetMelee(SetKnife)
        if game:GetService("Players").LocalPlayer.Data.Melee.Value == SetKnife then
            Rayfield:Notify({
                Title = "Melee Changer",
                Content = "Success your melee is now " .. SetKnife,
                Duration = 6.5,
                Image = 4483362458,
             })
            else
                Rayfield:Notify({
                    Title = "Melee Changer",
                    Content = "Error " .. SetKnife .. " dosent exist",
                    Duration = 6.5,
                    Image = 4483362458,
                 })
        end
    end,
    })

Appearance:CreateButton({
    Name = ("List Skins"),
    Callback = function()
        getgenv().ListSkins()
        game:GetService("StarterGui"):SetCore("DevConsoleVisible", true)
    end,
    })

SetKnife = game:GetService("Players").LocalPlayer.Data.Melee.Value

Appearance:CreateInput({
   Name = "Set Skin",
   CurrentValue = "",
   PlaceholderText = "Enter a Skin",
   RemoveTextAfterFocusLost = false,
   Flag = "Input1",
   Callback = function(Text2)
    SetSkin = Text2
   end,
})

Appearance:CreateButton({
    Name = ("Apply Skin"),
    Callback = function()
        getgenv().SetSkin(SetSkin)
        if game:GetService("Players").LocalPlayer.Data.Skin.Value == SetSkin then
            Rayfield:Notify({
                Title = "Skin Changer",
                Content = "Success your skin is now " .. SetSkin,
                Duration = 6.5,
                Image = 4483362458,
             })
            else
                Rayfield:Notify({
                    Title = "Skin Changer",
                    Content = "Error " .. SetSkin .. " dosent exist",
                    Duration = 6.5,
                    Image = 4483362458,
                 })
        end
    end,
    })

function bannedprompt()
-- Create ScreenGui
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "SimpleGui"
screenGui.Parent = game:GetService("CoreGui")
screenGui.DisplayOrder = 9999999

-- Create a Frame
local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 250, 0, 150)
frame.Position = UDim2.new(0.5, -125, 0.5, -75)
frame.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
frame.Parent = screenGui

-- Add a Title Text
local titleLabel = Instance.new("TextLabel")
titleLabel.Size = UDim2.new(1, 0, 0, 40)
titleLabel.Position = UDim2.new(0, 0, 0, 0)
titleLabel.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
titleLabel.Text = "Do you want to be banned?"
titleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
titleLabel.TextScaled = true
titleLabel.Font = Enum.Font.SourceSansBold
titleLabel.Parent = frame

-- Create Accept Button
local acceptButton = Instance.new("TextButton")
acceptButton.Size = UDim2.new(0, 100, 0, 40)
acceptButton.Position = UDim2.new(0.25, -50, 0.6, 0)
acceptButton.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
acceptButton.Text = "Yes"
acceptButton.TextColor3 = Color3.fromRGB(255, 255, 255)
acceptButton.TextScaled = true
acceptButton.Font = Enum.Font.SourceSans
acceptButton.Parent = frame

-- Create Deny Button
local denyButton = Instance.new("TextButton")
denyButton.Size = UDim2.new(0, 100, 0, 40)
denyButton.Position = UDim2.new(0.75, -50, 0.6, 0)
denyButton.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
denyButton.Text = "No"
denyButton.TextColor3 = Color3.fromRGB(255, 255, 255)
denyButton.TextScaled = true
denyButton.Font = Enum.Font.SourceSans
denyButton.Parent = frame

-- Add Functionality
acceptButton.MouseButton1Click:Connect(function()
    screenGui:Destroy() -- Remove GUI after acceptance
    getgenv().GetBanned()
end)

denyButton.MouseButton1Click:Connect(function()
    screenGui:Destroy() -- Remove GUI after denial
end)

end

Misc:CreateButton({
    Name = ("Get Banned"),
    Callback = function()
        bannedprompt()
    end,
    })

Main:CreateToggle({
   Name = "Bottomless Mag",
   CurrentValue = false,
   Flag = "Toggle1", -- A flag is the identifier for the configuration file, make sure every element has a different flag if you're using configuration saving to ensure no overlaps
   Callback = function(Value)
        if Value == true then
            getgenv().MagSize(999)
        else
            getgenv().resetstats("Ammo")
        end
   end,
})

Main:CreateToggle({
   Name = "Infinite Ammo",
   CurrentValue = false,
   Flag = "Toggle1", -- A flag is the identifier for the configuration file, make sure every element has a different flag if you're using configuration saving to ensure no overlaps
   Callback = function(Value)
        if Value == true then
            getgenv().StoredAmmo(999)
        else
            getgenv().resetstats("StoredAmmo")
        end
   end,
})

RoFMod = Main:CreateToggle({
    Name = "Rapid Fire",
    CurrentValue = false,
    Flag = "Toggle1", -- A flag is the identifier for the configuration file, make sure every element has a different flag if you're using configuration saving to ensure no overlaps
    Callback = function(Value)
         if Value == true then
            getgenv().FireRate(0.011)
            getgenv().FireType(true)
         else
             getgenv().resetstats("FireRate")
             getgenv().resetstats("Auto")
         end
    end,
 })

Main:CreateToggle({
    Name = "Perfect Accuracy",
    CurrentValue = false,
    Flag = "Toggle1", -- A flag is the identifier for the configuration file, make sure every element has a different flag if you're using configuration saving to ensure no overlaps
    Callback = function(Value)
         if Value == true then
            getgenv().Spread(0)
            getgenv().MaxSpread(0)
            getgenv().Recoil(0)
         else
             getgenv().resetstats("Spread")
             getgenv().resetstats("MaxSpread")
             getgenv().resetstats("RecoilControl")
         end
    end,
 })

Main:CreateToggle({
    Name = "Speed",
    CurrentValue = false,
    Flag = "Toggle1", -- A flag is the identifier for the configuration file, make sure every element has a different flag if you're using configuration saving to ensure no overlaps
    Callback = function(Value)
         if Value == true then
            getgenv().WalkSpeed(-500000)
         else
             getgenv().resetstats("Speed%")
         end
    end,
 })
